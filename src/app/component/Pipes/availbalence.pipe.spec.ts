import { AvailbalencePipe } from './availbalence.pipe';

describe('AvailbalencePipe', () => {
  it('create an instance', () => {
    const pipe = new AvailbalencePipe();
    expect(pipe).toBeTruthy();
  });
});
